describe("email plugin (integration)", () => {
  // Integration tests will be added as features are implemented.
  // Uses cds.test() pointed at test/test-app/ with kind: "basic".
  // See issue #15 for the full test case list.
});
